from setuptools import setup

# This is a minimal setup.py that uses pyproject.toml for configuration
# All package metadata and dependencies are now in pyproject.toml

if __name__ == "__main__":
    setup()
